import React from "react";
import { Avatar } from "./Avatar";

export const PostCard = ({ title, description, tags, date, image }) => {
  return (
    <div>
      <p>kasjdk</p>

      <Avatar date={date} />
    </div>
  );
};
